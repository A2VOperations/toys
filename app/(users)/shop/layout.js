export const metadata = {
  title: "Shop",
  description: "Browse our extensive collection of toys, educational stationery, school bags, and return gifts. Filter by category, age group, or brand to find the perfect gift for your child.",
};

export default function ShopLayout({ children }) {
  return <>{children}</>;
}
